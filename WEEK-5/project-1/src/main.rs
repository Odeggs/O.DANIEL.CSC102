fn main() {

    use std::io;
    
    println!("The values of a, b and c:");
    io::stdin().read_line(&mut String).parse().expect("Failed to read input");
    let mut input1 = String::new();
    let mut value:i32 =input1.trim()
}